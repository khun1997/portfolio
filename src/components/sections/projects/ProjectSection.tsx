import React from "react";

type Props = {};

const ProjectSection = (props: Props) => {
  return (
    <div id="projects" style={{ width: "100vw", height: "100vh" }}>
      ProjectSection
    </div>
  );
};

export default ProjectSection;
